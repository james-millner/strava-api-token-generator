management:
  endpoints:
    prometheus:
      enabled: true
    web:
      exposure:
        include: ["health", "info", "metrics", "trace", "prometheus"]

spring:
  servlet:
    multipart:
      max-file-size: 5MB


strava:
  accessToken: test
  clientId: test
  clientSecret: test
